using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class StartManager : MonoBehaviour
{
    public GameObject gameObject;
    // Start is called before the first frame update
    void Start()
    {
        //gameObject.transform.position = new Vector3(PlayerPrefs.GetFloat("ExamX"), PlayerPrefs.GetFloat("ExamY"), PlayerPrefs.GetFloat("ExamZ"));
        //gameObject.transform.localEulerAngles = new Vector3(PlayerPrefs.GetFloat("ExamTX"), PlayerPrefs.GetFloat("ExamTY"), PlayerPrefs.GetFloat("ExamTZ"));
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
